[package]
name = "baals-nodejs-native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
baals = { path = "../.." }
napi = { version = "2", features = ["napi4"] }
napi-derive = "2"
serde_json = "1.0"
hex = "0.4"

[build-dependencies]
napi-build = "2"
